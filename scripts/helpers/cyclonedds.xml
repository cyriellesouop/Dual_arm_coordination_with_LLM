<?xml version="1.0" encoding="UTF-8" ?>
<!--
  CycloneDDS configuration — kept minimal.
  With --network host, CycloneDDS uses multicast discovery by default
  and finds all peers on the LAN without explicit peer addresses.
  Only add <Peers> here if your network blocks multicast.
-->
<CycloneDDS xmlns="https://cdds.io/config"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
  <Domain id="any"/>
</CycloneDDS>
